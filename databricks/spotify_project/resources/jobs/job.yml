resources:
  jobs:
    Ingestion_job:
      name: Ingestion_job
      tasks:
        - task_key: ingestion
          notebook_task:
            notebook_path: ../../../../src/ingestion.ipynb
            base_parameters:
              catalog_name: spotify_catalogue
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: PERFORMANCE_OPTIMIZED